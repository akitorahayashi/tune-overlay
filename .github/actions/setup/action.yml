name: Setup
description: Common setup steps for Swift package workflows

inputs:
  setup_xcode:
    description: 'Whether to setup Xcode'
    required: true
  setup_ruby:
    description: 'Whether to setup Ruby'
    required: true

runs:
  using: composite
  steps:
    - name: Setup Xcode
      if: inputs.setup_xcode == 'true'
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '16.2'

    - name: Cache Mint packages
      uses: actions/cache@v4
      id: cache-mint-packages
      with:
        path: ${{ github.workspace }}/.mint
        key: ${{ runner.os }}-mint-${{ hashFiles('**/Mintfile') }}
        restore-keys: |
          ${{ runner.os }}-mint-

    - name: Install Mint
      run: brew install mint
      shell: bash

    - name: Install just
      uses: extractions/setup-just@v2

    - name: Setup Ruby
      if: inputs.setup_ruby == 'true'
      uses: ruby/setup-ruby@v1
      with:
        bundler-cache: true
